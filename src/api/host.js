// const host = "https://api.theprintribe.com/";

// const Url = {
//   customer: host + "api/customers/",
//   user:host+"api/users/"
// };

// export default Url;


const host = "http://localhost:500/";

const Url = {
  customer: host + "api/customers/",
  user: host + "api/users/"
};

export default Url;